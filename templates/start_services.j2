#!/usr/bin/env bash
# {{ ansible_managed }}

echo "Reloading/restarting services after certificate renewal"
systemctl reload nginx
systemctl restart postfix  
systemctl restart dovecot
systemctl restart spamassassin

